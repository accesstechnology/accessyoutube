version: '3'

services:
    php:
      build: .
      volumes:
            - .:/app
      restart: always
      container_name: youtube_scraper
      networks:
        - youtube

networks:
  youtube:
    external: true